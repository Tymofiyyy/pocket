# 🚀 Pocket Signals - Правильний запуск

## ⚠️ КРИТИЧНО ВАЖЛИВО!

**Admin Panel та Backend ПОВИННІ працювати на РІЗНИХ портах!**

- Backend: `http://localhost:3000` (API + uploads)
- Admin Panel: `http://localhost:8080` (UI)

## 🎯 Швидкий старт (Windows)

Просто запусти `START.bat` - він запустить обидва сервери автоматично!

## 📝 Ручний запуск

### 1. Запусти Backend (термінал 1)

```bash
cd backend
npm install  # тільки перший раз
npm start
```

Побачиш:
```
🚀 Pocket Signals Backend Starting...
📡 Server running on port 3000
```

### 2. Запусти Admin Panel (термінал 2)

```bash
cd admin-panel
node server.js
```

Побачиш:
```
🚀 Admin Panel Server Running
📍 URL: http://localhost:8080
```

### 3. Відкрий браузер

Відкрий **http://localhost:8080** ← САМЕ ЦЕЙ URL!

**НЕ відкривай http://localhost:3000** - це тільки для API!

## ✅ Перевірка що все працює

1. Відкрий http://localhost:8080
2. Створи новий ланцюжок
3. Додай крок з типом "Зображення"
4. Завантаж фото
5. **Фото повинно залишитись на екрані!**

## 🐛 Якщо проблеми

### Admin Panel не відкривається
- Перевір що `node server.js` запущений в папці `admin-panel`
- Перевір що порт 8080 вільний

### Фото не завантажуються
- Перевір що backend запущений (`npm start` в папці `backend`)
- Відкрий консоль браузера (F12) і подивись на помилки
- Перевір що папка `uploads` створилась

### Сторінка перезавантажується
- Переконайся що відкриваєш **http://localhost:8080**, а НЕ localhost:3000
- Почисти кеш браузера (Ctrl+Shift+Delete)

## 📂 Структура портів

```
localhost:3000  → Backend API
  ├─ /api/chains
  ├─ /api/upload
  ├─ /api/users
  └─ /uploads/    (статичні файли)

localhost:8080  → Admin Panel UI
  ├─ index.html
  └─ app.js
```

## 🔧 Технічні деталі

Backend (порт 3000):
- Express API сервер
- PostgreSQL база даних
- Multer для завантаження файлів
- CORS для cross-origin запитів

Admin Panel (порт 8080):
- Простий HTTP сервер (Node.js)
- Статичні HTML/CSS/JS файли
- Робить API запити на localhost:3000

## 💡 Порада

Додай в закладки:
- 📊 Admin Panel: http://localhost:8080
- 🔍 API Health: http://localhost:3000/health
